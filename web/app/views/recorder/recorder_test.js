'use strict';

describe('myApp.recorder module', function() {

  beforeEach(module('myApp.recorder'));

  describe('recorder controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var recorderCtrl = $controller('RecorderCtrl');
      expect(recorderCtrl).toBeDefined();
    }));

  });
});